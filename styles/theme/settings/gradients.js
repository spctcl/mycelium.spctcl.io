import colors from "./colors"

const gradients = {
  gradient: `linear-gradient(90deg, ${colors.brandPrimary} 0%, ${colors.brandSecondary} 64%, ${colors.brandTertiary} 100%)`,
}

export default gradients
